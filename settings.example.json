{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Starter ~/.claude/settings.json. Drop in as-is, or merge the 'hooks' block from hooks/hooks.json into your existing settings.",
  "permissions": {
    "allow": [
      "Bash(git status)",
      "Bash(git diff*)",
      "Bash(git log*)",
      "Bash(git branch*)",
      "Bash(ls*)",
      "Bash(pwd)",
      "Bash(node --version)",
      "Bash(npm --version)",
      "Bash(pnpm --version)"
    ],
    "deny": [
      "Bash(rm -rf /)",
      "Bash(git push --force*)",
      "Bash(git reset --hard*)"
    ]
  },
  "env": {
    "_comment": "Set DISABLE_TELEMETRY=1 to opt out of local session telemetry"
  },
  "_hooks_note": "To enable hooks, copy the 'hooks' block from coding-os/hooks/hooks.json into this file."
}
